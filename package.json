{
  "name": "temperature_comparison",
  "version": "1.0.0",
  "description": "app for comparing a country's temperature over time",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mike",
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^2.6.0"
  }
}
